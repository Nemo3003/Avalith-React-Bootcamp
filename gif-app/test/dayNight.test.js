import {DayNight} from '../../src/helpers/dayNight';

describe('changing colors on the day and at night', function () {
    test('changing colors on the day and at night', function () {
        const day = jest.fn();
        const night = jest.fn();
        
    })
})